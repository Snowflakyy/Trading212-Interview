spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/trading212
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: trading212-interview
server:
  port: 8080